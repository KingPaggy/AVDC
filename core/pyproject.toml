[project]
name = "avdc-core"
version = "0.1.0"
description = "AVDC core: JAV metadata scraping and organizing logic"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["test"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
